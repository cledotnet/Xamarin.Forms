<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Cleveland.DotNet.Sig.DiabetesLog.Views;assembly=DiabetesLog"
             xmlns:vm="clr-namespace:Cleveland.DotNet.Sig.DiabetesLog.ViewModels;assembly=DiabetesLog"
             x:TypeArguments="vm:MealPageModel"
             x:Class="Cleveland.DotNet.Sig.DiabetesLog.Views.MealPage">
  <StackLayout Orientation="Vertical">
    <Label Text="{Binding Path=Title}" FontAttributes="Bold"/>
    <Label Text="{Binding Path=Introduction}"/>
    <StackLayout Orientation="Horizontal">
      <Label Text="Timestamp: "/>
      <DatePicker Date="{Binding Path=Meal.Timestamp}"/>
      <TimePicker Time="{Binding Path=Meal.Timestamp}"/>
    </StackLayout>
    <StackLayout Orientation="Horizontal">
      <Label Text="Name: "/>
      <Entry Text="{Binding Path=Meal.Name}" WidthRequest="200"/>
    </StackLayout>
    <StackLayout Orientation="Horizontal">
      <Label Text="Carbohydrates: "/>
      <Entry Text="{Binding Path=Meal.Carbohydrates}" WidthRequest="100"/>
    </StackLayout>
  </StackLayout>

</views:BasePage>